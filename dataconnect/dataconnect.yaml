specVersion: "v1beta"
serviceId: "handyman-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "handyman-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
